{
  "name": "newtcommander-web",
  "version": "0.1.0",
  "private": true,
  "description": "Website for Newt Commander, an open source two-pane file manager. Hosted on Cloudflare Workers.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
